apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: nginx-release
  namespace: nginx
spec:
  chart:
    spec:
      chart: nginx-ingress
      version: 2.x.x
      sourceRef:
        kind: HelmRepository
        name: nginx
      interval: 1h
  interval: 1m0s
  releaseName: nginx